# r1mx toolkit — direct Python dependencies
# Generated from actual imports in toolkit/
#
# pcbnew (KiCad API) is NOT listed here — it is a system package accessed via
# .venv/lib/python3.13/site-packages/kicad.pth -> /usr/lib/python3/dist-packages
# Install KiCad to get it: sudo apt install kicad

# --- GUI ---
PyQt6==6.11.0
PyQt6-Qt6==6.11.0
PyQt6_sip==13.11.1

# --- Computer vision / OCR ---
numpy==2.3.5
opencv-contrib-python==4.10.0.84
Pillow==12.2.0
easyocr==1.7.2
pytesseract==0.3.13

# --- AI / embeddings ---
chromadb==1.5.9
fastembed==0.8.0

# --- MCP server ---
mcp==1.27.1

# --- Datasheets / web ---
beautifulsoup4==4.14.3
requests==2.33.1
svgpathtools==1.7.2

# --- Utilities ---
tqdm==4.67.3

# --- Testing ---
pytest==9.0.3
